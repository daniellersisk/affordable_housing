# Package marker for external API client modules.
# Clients handle communication with external systems only (e.g. NYC Open Data).
# Do not use a client pattern for database access; use repositories instead.
