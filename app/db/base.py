# Declarative base for all SQLAlchemy ORM models.
# All models must inherit from Base so Alembic can detect them during autogenerate.
from __future__ import annotations

# TODO: Step 3 - define Base = DeclarativeBase() here
# TODO: Step 3 - import all models in alembic/env.py so migrations detect schema changes
