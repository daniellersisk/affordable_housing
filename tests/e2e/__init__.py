# Package marker for end-to-end / system tests.
# E2E tests call the API over HTTP against the running containerized stack.
# Requires docker compose up --build before running; see tests/e2e/README.md.
