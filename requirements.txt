# production runtime deps only.
# kept separate from requirements-dev.txt so a prod image can install just this
# file and not ship pytest, ruff, or any dev tooling into production.
# in a multi-stage dockerfile the runtime stage would: pip install -r requirements.txt
fastapi==0.136.0
uvicorn[standard]==0.45.0
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.3
httpx==0.28.1
